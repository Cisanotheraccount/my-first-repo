{
  "name": "cdw-nyc-housing-guide-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/housing-context.js && node --test",
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "wrangler": "4.118.0"
  }
}
